type Current @entity {
  id: ID!
  address: Bytes!
  block: BigInt!
  timestamp: BigInt!
}

type PreviousHolder @entity {
  id: ID!
  address: Bytes!
  tiempo: BigInt! # por cuanto tiempo holdeo el viajero
  block: BigInt!
  timestamp: BigInt!
}
